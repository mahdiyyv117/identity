Install the module on Python

cmd:
py -3 setup.py
py -3 -m pip install identity-1.0.tar.gz

linux terminal:
python3 setup.py
python3 -m pip install identity-1.0.tar.gz

mac terminal:
python setup.py
python -m pip install identity-1.0.tar.gz
